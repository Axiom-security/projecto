// AUTOGENERATED FILE

// +build !codeanalysis

package [[.Name]]

import ( 
	"projecto/db"
	"projecto/model"
)

type i[[.Repo]]Repo interface {
    Find() ([][[.Model]], error)
    Create([[.Model]]) error
}

func newRepo[[.Repo]](db *db.Database) *repo[[.Repo]] {
	return &repo[[.Repo]]{
		db: db,
	}
}

type repo[[.Repo]] struct {
	db *db.Database
}

func (r *repo[[.Repo]]) Find() ([][[.Model]], error) {
    records := [][[.Model]]{}
	if err := r.db.GetDatabase().Find(&records).Error; err != nil {
      return records, err
    }
    return records, nil 
}


func (r *repo[[.Repo]]) Create(record [[.Model]]) (error) {
	return r.db.GetDatabase().Create(&record).Error
}

